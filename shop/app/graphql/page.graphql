query Page($slug: String!, $locale: LocaleInputType!) {
  Pages(where: { slug: { equals: $slug } }, locale: $locale) {
    docs {
      id
      title
      content
      slug
      publishedOn
      updatedAt
      meta {
        title
        description
        image {
          url
          alt
        }
      }
      conversionBoxes {
        title
        description
        image {
          url
          alt
        }
        id
      }
    }
  }
}
